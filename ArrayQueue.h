#pragma once

#include <iostream>
#include <exception>
#include "StackException.h"

template <class T>
class ArrayQueue {
public:
    ArrayQueue();
    ArrayQueue(const ArrayQueue& orig);
    virtual ~ArrayQueue();
private:

};
 
