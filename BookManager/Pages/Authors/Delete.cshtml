@page
@model DeleteModel
@{
        ViewData["Title"] = "Delete a Author";
        var id = Model.Entry.Id;
        var title = Model.Entry.FullName;
}
 
    <form method="post"> 
        <p>Confirm delete Author:<b> @title </b></p>
        <button type="submit" asp-route-id="@id" class="button greenBk">Delete</button>
        <a asp-page="./Index" class="button redBk">Cancel</a>
    </form>